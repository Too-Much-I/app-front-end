# Claude Code instructions

  Read and follow [AGENTS.md](./AGENTS.md) before making changes.

  Critical rules:

  - Use `pnpm`.
  - This is an Expo/React Native app.
  - Use shared `Text` and `Pressable` components.
  - Run lint and TypeScript validation before handoff.

  `AGENTS.md` is the single source of truth. If these files conflict, follow `AGENTS.md`.
